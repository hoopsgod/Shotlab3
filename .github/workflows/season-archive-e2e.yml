name: Season Archive E2E

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  season-archive-browser-flow:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install application dependencies
        run: npm ci

      - name: Run season archive service tests
        run: |
          set +e
          node --test tests/season-archive.test.mjs tests/season-archive-react-compat.test.mjs tests/season-archives-route.test.mjs > season-archive-service-tests.txt 2>&1
          status=$?
          cat season-archive-service-tests.txt
          exit $status

      - name: Install Playwright test runner
        run: npm install --no-save @playwright/test@1.55.1

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Build application
        run: npm run build

      - name: Run season archive browser test
        run: npm run test:e2e:season-archive

      - name: Run player career history browser test
        run: npx playwright test tests/e2e/player-career-history.spec.mjs --config=playwright.acceptance.config.mjs

      - name: Run complete new season wizard flow
        run: npx playwright test tests/e2e/new-season-wizard.spec.mjs

      - name: Run visual hierarchy browser test
        run: npm run test:e2e:visual-hierarchy

      - name: Run mobile navigation browser test
        run: npm run test:e2e:mobile-navigation

      - name: Run semantic color browser test
        run: npm run test:e2e:semantic-colors

      - name: Upload browser failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: season-archive-hierarchy-navigation-semantic-failure-artifacts
          path: |
            season-archive-service-tests.txt
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 7
