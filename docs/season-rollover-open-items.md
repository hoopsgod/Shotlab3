# Rollout gates

Before merge:

- required CI passes;
- migration reviewed and applied to the target Supabase project;
- wizard mounted in the coach-only season-management surface;
- registered-coach smoke test passes;
- duplicate submission produces one season;
- archive and leaderboard regressions pass.
