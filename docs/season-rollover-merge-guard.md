# Merge guard

Do not merge until the wizard is mounted in the coach-only season-management surface, migration 034 is applied to the target Supabase project, all required CI checks pass, and a registered-coach smoke test confirms idempotent creation with zero carried-forward statistics.
